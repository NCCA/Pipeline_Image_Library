
#include "CollisionDetector.h"


void CollisionDetector::detectCollisions(std::vector<RigidBody*>& bodies) {

    for (size_t i = 0; i < bodies.size(); ++i) {
        for (size_t j = i + 1; j < bodies.size(); ++j) {

        }
    }
}